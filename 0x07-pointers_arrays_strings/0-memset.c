#include "main.h"

/**
*_memset - Fills memory with a constant byte
* @s: pointer
* @b: char
* @n: unsigned int
*Return: s
*/

void *_memset(void *s, int c, size_t n)
{
		unsigned int index;
	for (index = 0; n > 0; index++)
{
	s(index) = b;
	n -= 1;
}
return (s);
}
